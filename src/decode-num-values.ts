/**
 * Decode the string where each line contains obscured numerical values.
 * 
 * The hidden numerical value on each line ca be found by combining the first digit and the last digit to form a 
 * single two-digit number. However, some digits are written in words.
 * 
 * ---
 * four9xthree (-> 43)
 * abcnineight12 (-> 92)
 * ---
 * 
 * The function must return the sum of those hidden numerical values.
 * 
 * @param data string to be decoded
 */
export function decodeNumValues(data: string): number {
    return 0;
}
